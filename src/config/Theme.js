const DarkColor = {
  text: 'white',
  background: 'black',
};

const LightColor = {
  text: 'black',
  background: 'white',
};

const Theme = {
  dark: {
    ...DarkColor,
  },
  light: {
    ...LightColor,
  },
};

export default Theme;
